# Drill 8 — RBAC-style permission checks (25 min)

**Time box:** 25 minutes.

## What can go away in ~30 minutes

- OAuth, JWT parsing, attribute-based access (resource IDs), policy engines.
- Keep **static role-to-action table** and an **audit list**.

## What they are testing

- **Data-driven authorization** (easy to extend, easy to read).
- **Audit trail** as a first-class output, not an afterthought.

## Your task

Implement `practice.py` until `python practice.py` prints `ok`.

## Stuck?

See `reference.py`.
